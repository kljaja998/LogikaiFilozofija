<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kljaja.logikaifilozofija">

    <application
        android:allowBackup="true"
        android:icon="@drawable/head_gears_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".izborStraneActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".izborAktivnostiActivity"
            android:parentActivityName=".izborStraneActivity" />
        <activity android:name=".ucenjeActivity" />
        <activity android:name=".vezbanjeActivity" />
        <activity
            android:name=".LoadingActivity"
            android:theme="@style/AppTheme.NoActionBar"
                >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>